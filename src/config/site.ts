/**
 * Single source of truth for the site owner's public details.
 * Anything hardcoded in more than one place belongs here so the copies stay in sync.
 */
export const CONTACT_EMAIL = 'henry@bao.dev';
